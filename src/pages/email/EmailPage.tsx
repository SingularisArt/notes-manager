import Topbar from "../../components/common/Topbar";

type Props = {};

const EmailPage = (props: Props) => {
  return (
    <div>
      <Topbar title="Email" />
    </div>
  );
};

export default EmailPage;
